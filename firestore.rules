rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // In a real app we'd restrict to auth, but for this demo synchronicity is key.
    }
  }
}
